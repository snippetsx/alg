#include <b_tree.h>

int main()
{
    btree_t *root;
    b_insert(&root, 20);
    b_insert(&root, 16);
    b_insert(&root, 28);
    b_insert(&root, 50);
    b_insert(&root, 3);
    b_insert(&root, 42);
    b_insert(&root, 35);
    b_insert(&root, 44);
    b_insert(&root, 76);
    b_insert(&root, 24);

    
}
